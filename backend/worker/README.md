# Cloudflare Worker API

This is the backend API for BLINTZY, running on Cloudflare Workers and connecting to D1.

## Local Development
To run this worker locally, you can use Wrangler from the project root:

```bash
npx wrangler dev
```

Make sure you have created `.dev.vars` in the root directory with the appropriate environment variables.
